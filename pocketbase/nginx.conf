# Nginx Reverse Proxy Config untuk PocketBase
# Simpan sebagai nginx.conf di folder yang sama dengan docker-compose.yml

server {
    listen 80;
    server_name api.motifkain.com;

    # Redirect HTTP to HTTPS (uncomment setelah SSL ter-install)
    # return 301 https://$server_name$request_uri;

    location / {
        proxy_pass http://pocketbase:8090;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Timeouts
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }
}

# HTTPS Server (uncomment setelah SSL ter-install)
# server {
#     listen 443 ssl http2;
#     server_name api.motifkain.com;
#
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     location / {
#         proxy_pass http://pocketbase:8090;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
# }
